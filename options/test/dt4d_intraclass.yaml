# general setting
name: dt4d_intra
backend: dp # DataParallel
type: FeatContrastiveFMNetModel
num_gpu: 1 # 
gpu_idx: 0 # 
manual_seed: 1234
non-isometric: false
partial: false
visualize: false

# path
path:
  resume_state: checkpoints_ours/dt4d.pth 
  resume: false

# datasets
datasets:
  test_dataset:
    name: Val
    type: PairDT4DDataset
    phase: test
    data_root: ../data/DT4D_r/
    inter_class: false
    return_evecs: true
    return_faces: true
    num_evecs: 200
    return_corr: true
    return_dist: true

# network setting
networks:
  feature_extractor:
    type: DiffusionNet
    in_channels: 16
    out_channels: 128
    cache_dir: ../data/DT4D_r/diffusion
    input_type: hks
  permutation:
    type: Similarity
    tau: 0.07
    hard: true

# validation setting
val:
  metrics:
    geo_error:
      type: calculate_geodesic_error
    plot_pck:
      type: plot_pck
